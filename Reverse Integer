## 
Given a 32-bit signed integer, reverse digits of an integer.

Example 1:

Input: 123
Output: 321
##

class reverse:
    def ReverseOfNumbers(self, num):
        a = int(input("enter a number:"))
        rev = 0
        while (a > 0):
            rev = (rev * 10 + a % 10)
            a = a // 10
            print ('reverse=', rev)
